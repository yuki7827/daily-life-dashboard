# Routine実行時の指示

このリポは Claude Code Routines により毎朝6:30 JSTに自動実行され、当日の生活ダッシュボードを生成・公開する。

## あなたの役割

実行日（JST）の生活ダッシュボードを `_posts/YYYY-MM-DD-daily.md` として生成し、main ブランチにコミット＆プッシュする。

## 厳守ルール

- **書き込み可能**: `_posts/`, `index.md`（必要時のみ）, `archive.md`（必要時のみ）
- **書き込み禁止**: `CLAUDE.md`, `_config.yml`, `_layouts/`, `data/`, `README.md`
- **機密情報・APIキー・個人情報は一切扱わない／生成物に含めない**
- 各セクションは**取得失敗時は「（情報取得できず）」と短く記載してスキップ**。途中で止まらない
- 生成された情報には必ず**取得元URLか「(出典: 〜)」表記**を添える

## 情報取得の方針（重要）

**ローカル実行のためネットワーク制限なし。WebFetch を積極的に使い、詳細情報を取得する。**

- **まず WebFetch** で `data/sources.yml` の URL を直接取得する
- WebFetch が 403 / タイムアウト等で失敗した場合のみ WebSearch にフォールバック
- WebSearch でも取得できない場合は「（情報取得できず）」と記載してスキップ（止まらない）
- `data/sources.yml` の URL を起点に、リンク先の詳細ページまで掘り下げて価格・品名を取得する

## 実行フロー

### 1. 実行日と居住地の判定
- JSTの今日の日付を取得（`TZ=Asia/Tokyo date +%F`）
- `data/config.yml` の `location_switch_date` と比較
  - 今日 < 切替日 → `hachimanyama`（八幡山）
  - 今日 ≥ 切替日 → `fujimino`（ふじみ野）
- 該当ロケーションの `weather_area_code` 等を保持

### 2. 各セクションの情報収集（順序通り）

`data/sources.yml` を店舗名・自治体名等の参考として使いつつ、**WebSearch → 必要時のみ WebFetch** の順で取得する。各セクションは独立して try/skip。

**(1) 天気・洗濯・花粉**
- WebSearch `"東京 天気予報 今日 YYYY年M月D日"` または `"世田谷区 天気 今日"` で最高/最低気温・降水確率・天気概況を取得
- 洗濯指数: WebSearch `"東京 洗濯指数 今日"` で取得。取得できなければ降水確率から推定（30%未満=◎、30-60%=△、60%以上=×）
- 花粉: **最初の並列fetchバッチに必ず** `sources.yml` の `tenki_pollen_tokyo`（または `tenki_pollen_saitama`）を WebFetch で取得し、今日の飛散レベル（少・やや多・多・非常に多）を数値で記載する。WebFetch 失敗時のみ WebSearch `"東京 花粉 飛散 今日"` にフォールバック（時期外はオフシーズン表記）
- ※ fujimino の場合は「埼玉県南部」「ふじみ野市」に読み替え

**(2) スーパー特売・チラシ**

**目的: 「今日お得に買えるもの」「今日のおすすめ品」を価格付きで提示する。チラシ期間の記載は不要。**

- `data/sources.yml` の各店舗 URL（tokubai / shufoo / kurashiru）を WebFetch で取得し、**本日有効な特売商品を品名・価格で5〜8件**列挙する
- 価格が取得できた場合は必ず記載（例: 国産豚バラ 100g **158円**）
- 価格不明の場合は「価格はアプリで確認」と添える
- 「今日のイチオシ」として最もお得な商品を1〜2件ピックアップしてセクション冒頭に記載
- WebFetch 失敗時は WebSearch `"店舗名 今日 特売 円"` でフォールバック
- ※ fujimino の場合はヤオコー・マルエツ・ベルクに読み替え

**(3) ファミレス・外食キャンペーン**

**目的: 新メニューより「割引・クーポン・お得キャンペーン」を優先して掲載する。**

- 各チェーンのキャンペーンページ・クーポン情報を WebFetch で確認する
- 掲載優先順: ① 割引・クーポン（金額・%OFF明記） ② 期間限定お得セット ③ 新メニュー（お得要素があるもののみ）
- 「何円お得か」「どう使うか（アプリ提示・レシート等）」を明記
- 価格比較できる場合は通常価格→割引後価格を記載（例: 通常740円 → **490円**）
- 情報がない or 新メニューしかないチェーンは省略してOK（掲載数より質を優先）

**(4) 今日の献立提案**

**目的: (2)で取得したスーパー特売品を主役にした献立を提案する。**

- **(2)の特売品を必ず使う**献立を2パターン提案する。特売品が取得できていない場合は天気・季節から提案
- 各案の形式:
  - **使う特売品**: 「〇〇（〇〇円）を活用」と明示
  - **主菜・副菜・汁物**: それぞれ1行で
  - **追加の買い物リスト**: 特売品以外に買い足すものだけ列挙
- 旬の食材・気温（暑い日は冷たいもの等）も考慮する

**(5) ゴミ収集（当日／明日）**

**対象地区: 世田谷区上北沢5丁目（固定）**

- **取得順序**: まず WebFetch で HTML ページ `https://www.city.setagaya.lg.jp/02241/416.html` を取得する。失敗した場合のみ PDF（`syusyubi.pdf`）を試みる（PDF は画像化されていることがあり取得失敗しやすい）
- 上北沢5丁目の収集曜日・品目を確認する（可燃: 月・木、資源: 土、不燃: 第2・4火、PET: 第1・3火）
- **当日と翌日の収集品目**を曜日と合わせて記載（例: 今日（月）: 可燃ごみ）
- **「第N回目の曜日」が必要な品目（不燃ごみ・ペットボトル）は実行日から計算して今週・来週の該当日を明示する**
  - 例: 実行日が5/10(日)なら、翌5/12(火)が「5月第2火曜日」→ 不燃ごみ収集日
  - 計算方法: その月の第1日(1日)から実行日までに対象曜日が何回出現したかを数える
- 祝日の場合は振替有無を確認して記載
- 不明な場合は「（要確認）」と添えて公式URLを案内
- search query: `"世田谷区 ごみ 収集日 上北沢 曜日"` または `"世田谷区 ごみと資源 収集日カレンダー 上北沢"`
- ※ fujimino に切替後は「ふじみ野市 ゴミ収集 曜日」で検索

**(6) 近隣イベント**
- WebSearch で今日〜今週末のイベント・新店オープンを2〜3件取得する
- **検索クエリは「世田谷区 + 近隣駅名」で指定する**（「八幡山」単体だと栃木・滋賀の同名地名が混入するため）
  - 例: `"世田谷区 八幡山 OR 上北沢 OR 千歳烏山 イベント YYYY年M月"` や `"世田谷区 イベント 今週 YYYY年M月D日"`
- ※ fujimino の場合は「ふじみ野市 OR 上福岡 OR ふじみ野駅 イベント YYYY年M月」で検索

### 3. 出力ファイル生成

`_posts/YYYY-MM-DD-daily.md` を以下のfront matterで作成：

```yaml
---
layout: post
title: "YYYY年M月D日の朝のダッシュボード"
date: YYYY-MM-DD 06:30:00 +0900
location: "（locationのlabel）"
---
```

本文はMarkdown。各セクションは `## 1. 天気・洗濯・花粉` のようにナンバリングしたh2見出しで開始。

末尾に「## ⚙ 取得状況」セクションを設け、各セクションの取得成否を一覧で記録。

### 4. コミット・プッシュ

- `git add _posts/YYYY-MM-DD-daily.md`
- 既存の同日付ファイルがあれば上書き
- コミットメッセージ: `daily: YYYY-MM-DD dashboard`
- `git push origin main`

## 失敗時の挙動

- 情報取得が全滅した場合でも、空のセクション付きでファイルを生成して push する（途中失敗を見える化）
- gitプッシュ自体に失敗した場合のみエラー終了

## 開発時の確認

ローカルで動作を試したいとき：
- `bundle exec jekyll serve` でプレビュー
- `_posts/YYYY-MM-DD-daily.md` を手書きしてレイアウト確認も可

## 参考

- セットアップガイド: https://github.com/yuki7827/claude-code-ios-daily/blob/main/docs/routine-setup-guide.md
