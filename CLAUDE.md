# Routine実行時の指示

このリポは毎朝ローカルのスケジュールタスクで実行され、当日の生活ダッシュボードを生成・公開する。
居住地: **埼玉県富士見市**（最寄り ふじみ野駅）。

## あなたの役割

実行日（JST）の生活ダッシュボードを `_posts/YYYY-MM-DD-daily.md` として生成し、main ブランチにコミット＆プッシュする。

## 厳守ルール

- **書き込み可能**: `_posts/`, `index.md`（必要時のみ）, `archive.md`（必要時のみ）
- **書き込み禁止**: `CLAUDE.md`, `_config.yml`, `_layouts/`, `data/`, `README.md`
- **機密情報・APIキー・個人情報は一切扱わない／生成物に含めない**
- 各セクションは**取得失敗時は「（情報取得できず）」と短く記載してスキップ**。途中で止まらない
- 生成された情報には必ず**取得元URLか「(出典: 〜)」表記**を添える

## 情報取得の方針（重要）

**ローカル実行のためネットワーク制限なし。WebFetch を積極的に使い、詳細情報を取得する。**

- **まず WebFetch** で `data/sources.yml` の URL を直接取得する
- WebFetch が 403 / タイムアウト等で失敗した場合のみ WebSearch にフォールバック
- WebSearch でも取得できない場合は「（情報取得できず）」と記載してスキップ（止まらない）
- `data/sources.yml` の URL を起点に、リンク先の詳細ページまで掘り下げて価格・品名を取得する

## 実行フロー

### 1. 実行日の取得
- JSTの今日の日付・曜日を取得（`TZ=Asia/Tokyo date "+%F (%a)"`）
- 居住地は富士見市で固定（切替ロジックは廃止）

### 2. 各セクションの情報収集

`data/sources.yml` を参照し、**WebFetch → 必要時 WebSearch** の順で取得。各セクションは独立して try/skip。

**(1) 天気・洗濯・花粉**
- 気象庁JSON（埼玉県南部）または tenki.jp で最高/最低気温・降水確率・天気概況を取得
- 洗濯指数: 取得できなければ降水確率から推定（30%未満=◎、30-60%=△、60%以上=×）
- 花粉: 飛散レベルを取得（時期外はオフシーズン表記）

**(2) スーパー特売・チラシ**

**目的: 「今日お得に買えるもの」「今日のおすすめ品」を価格付きで提示する。チラシ期間の記載は不要。**

- `data/sources.yml` の各店舗 URL（tokubai）を WebFetch し、**本日有効な特売商品を品名・価格で5〜8件**列挙
- 価格が取得できた場合は必ず記載（例: 国産豚バラ 100g **158円**）／不明なら「価格はアプリで確認」
- 「今日のイチオシ」として最もお得な1〜2件をセクション冒頭に
- WebFetch 失敗時は WebSearch でフォールバック

**(3) ファミレス・外食キャンペーン**

**目的: 新メニューより「割引・クーポン・お得キャンペーン」を優先。**

- 各チェーンのキャンペーン・クーポン情報を WebFetch で確認
- 優先順: ① 割引・クーポン（金額・%OFF明記） ② 期間限定お得セット ③ お得要素のある新メニュー
- 「何円お得か」「使い方（アプリ提示・レシート等）」を明記（例: 通常740円 → **490円**）
- 割引情報がない/新メニューのみのチェーンは省略してOK（数より質）

**(4) オンラインショップお得情報**

**目的: 楽天市場・Yahoo!ショッピング等の「今日が買い時か」を伝える。**

- `data/sources.yml` の `online_shopping` を参照
- 本日開催/直近のセール・ポイント還元を掲載:
  - 楽天: お買い物マラソン / スーパーSALE / 0と5のつく日 / 勝ったら倍 等
  - Yahoo: 5のつく日 / ゾロ目の日 / PayPayポイント還元
  - Amazon: タイムセール祭り・プライムデー等の大型セール時のみ
- 「今日はポイント◯倍デー」のように、今日買うべきか一言で判定
- 大型セール開催中なら冒頭で強調

**(5) キャッシュレス・アプリお得情報**

**目的: バーコード決済・スーパー/飲食店公式アプリのお得情報を伝える。**

- `data/sources.yml` の `cashless_apps` を参照
- 掲載対象:
  - PayPay/d払い/楽天ペイ/au PAY の還元キャンペーン（特に**埼玉県・富士見市の自治体還元**は最優先で確認）
  - 近隣スーパー（ヤオコー/マルエツ/ベルク）のアプリ限定クーポン・ポイントデー
  - 飲食チェーン公式アプリのクーポン
- 「今日が高還元日か」「どのアプリ/決済が得か」を端的に

**(6) 今日の献立提案**

**目的: (2)で取得したスーパー特売品を主役にした献立を提案する。**

- **(2)の特売品を必ず使う**献立を2パターン。特売品が取得できていない場合は天気・季節から提案
- 各案の形式:
  - **使う特売品**: 「〇〇（〇〇円）を活用」と明示
  - **主菜・副菜・汁物**: それぞれ1行
  - **追加の買い物リスト**: 特売品以外に買い足すものだけ
- 旬の食材・気温（暑い日は冷たいもの等）も考慮

**(7) ゴミ収集（当日／明日）**

**富士見市・自宅地区。`data/config.yml` の `garbage_schedule`（静的）を使う。Web取得は不要。**

- 今日と明日の曜日から `garbage_schedule` を引いて品目を記載:
  - 月: 不燃・有害ごみ / ペットボトル / 紙・布類 / ビン / カン
  - 水・土: 可燃ごみ
  - 水: 資源プラスチック
  - 火・木・金・日: 収集なし
- 末尾に「詳細は[富士見市ごみ分別パンフレット（PDF）](https://www.city.fujimi.saitama.jp/kurashi_tetsuzuki/gomi_recycle/gomi/dashikata/gominobunbtunituite.files/R7_pamphlet.pdf)で確認」と案内する
- 祝日でも基本この静的スケジュール通りに記載（特別な変更が分かる場合のみ補足）

**(8) 近隣イベント**
- `data/sources.yml` の `events` を参照し、今日〜今週末のイベント・新店オープンを2〜3件
- ふじみ野駅周辺（ソヨカふじみ野・ららぽーと富士見等）の催事も対象

### 3. 出力ファイル生成

`_posts/YYYY-MM-DD-daily.md` を以下のfront matterで作成：

```yaml
---
layout: post
title: "YYYY年M月D日の朝のダッシュボード"
date: YYYY-MM-DD 06:30:00 +0900
location: "埼玉県富士見市"
---
```

本文はMarkdown。各セクションは `## 1. 天気・洗濯・花粉` のようにナンバリングしたh2見出しで開始。
末尾に「## ⚙ 取得状況」セクションを設け、各セクションの取得成否を一覧で記録。

### 4. コミット・プッシュ

- `git add _posts/YYYY-MM-DD-daily.md`
- 既存の同日付ファイルがあれば上書き
- コミットメッセージ: `daily: YYYY-MM-DD dashboard`
- `git push origin main`

## 失敗時の挙動

- 情報取得が全滅した場合でも、空のセクション付きでファイルを生成して push する
- gitプッシュ自体に失敗した場合のみエラー終了

## 参考

- セットアップガイド: https://github.com/yuki7827/claude-code-ios-daily/blob/main/docs/routine-setup-guide.md
